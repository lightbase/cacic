{% extends 'CacicRelatorioBundle::base.html.twig' %}

{% block body %}

    <div class="row-fluid">
        <div class="span12">
            <div class="box grad_colour_black">
                <h2 class="box_head round_top"><i class="icon-list"></i> {{'Histórico de Usuário Logado' |trans }}</h2>
                <div class="block box_content round_bottom padding_10">
                    <h5>{{ 'Relatório gerado em'|trans }} {% if idioma == 'pt_BR' %}{{ "now"|date("d/m/Y H\\hi") }}{% else %}{{ "now"|date("m/d/Y H\\hi") }}{% endif %}</h5>

                    <hr />

                    <table class="table table-striped">

                        <thead>
                        <tr>
                            <th width="20%" style="text-align: center">{{ "Usuário"|trans }}</th>
                            <th width="30%" style="text-align: center">{{ "Data"|trans }}</th>
                        </tr>
                        </thead>

                        <tbody>
                        {% for log in dados %}

                            <tr>
                                <td>{{ log['usuario'] }}</td>
                                <td>{{ log['data'] }}</td>
                            </tr>

                        {% else %}
                            <tr>
                                <td style="text-align: center;" colspan="6"><b>{{ "NENHUM REGISTRO ENCONTRADO!"|trans }}</b></td>
                            </tr>
                        {% endfor %}

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

{% endblock %}