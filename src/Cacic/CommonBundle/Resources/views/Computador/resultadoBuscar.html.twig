<div class="row-fluid">
    <div class="span12">
        <div class="box grad_colour_black">
            <h2 class="box_head round_top"><i class="icon-list"></i> {{'Resultado da pesquisa' |trans }}</h2>
            <div class="block box_content round_bottom padding_10">

                <table class="table table-striped table-bordered">
                    <thead>
                    <tr>
                        <th width="30%" style="text-align: center">{{ "Nome da Máquina"|trans }}</th>
                        <th width="30%" style="text-align: center">{{ "Endereço IP"|trans }}</th>
                        <th width="25%" style="text-align: center">{{ "Versão Cacic"|trans }}</th>
                        <th width="35%" style="text-align: center">{{ "Endereço MAC"|trans }}</th>
                        <th width="5%"></th>
                    </tr>
                    </thead>

                    <tbody>

                    {% for computador in computadores %}

                        <tr>
                            <td style="text-align: center">{{ computador['nmComputador'] }}</td>
                            <td style="text-align: center">{{ computador['teIpComputador'] }}</td>
                            <td style="text-align: center">{{'Versão ' |trans }} {{ computador['teVersaoCacic'] }}</td>
                            <td style="text-align: center">{{ computador['teNodeAddress'] }}</td>
                            <td>
                                <a class="btn" href="{{ path("cacic_computador_detalhar") }}/{{ computador['idComputador'] }}" target="_blank">
                                    <i class="icon-search"></i>
                                </a>
                            </td>
                        </tr>

                    {% else %}
                        <tr>
                            <td style="text-align: center;" colspan="5"><b>{{ "NENHUM REGISTRO ENCONTRADO!"|trans }}</b></td>
                        </tr>
                    {% endfor %}

                    </tbody>
                </table>

            </div> <!-- /block -->
        </div> <!-- /box -->
    </div> <!-- /span -->
</div> <!-- /row -->